{
  "homepage": "https://eddhall0821.github.io/gunfire",
  "type": "module",
  "scripts": {
    "dev": "nodemon index.js",
    "build": "babel -d build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "nodemon": "^3.0.1"
  }
}
